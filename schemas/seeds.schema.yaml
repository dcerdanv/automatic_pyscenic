$schema: "http://json-schema.org/draft-07/schema#"

description: each row of seeds.tsv represents a pyscenic seed

properties:
    seed_ID:
        type: integer
        description: pyscenic seed

required:
    - seed_ID
